<!-- BEGIN:nextjs-runtime-rules -->

# Next.js Runtime Notes

This project uses a Next.js version with current APIs, conventions, and file structure. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before changing framework-specific code. Heed deprecation notices.

<!-- END:nextjs-runtime-rules -->